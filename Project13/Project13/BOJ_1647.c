#include <stdio.h>

int arr[100001];

int main() {
	int n, m, a, b, c;
	freopen("input.txt", "r", stdin);
	scanf("%d %d", &n, &m);
	for (int i = 1; i <= n; i++) {
		arr
	}
	for (int i = 0; i < m; i++) {
		scanf("%d %d %d", &a, &b, &c);

	}
}